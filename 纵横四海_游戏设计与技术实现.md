# 《纵横四海》文字 RPG：游戏设计与技术实现蓝图

> 文档定位：用于制作一款保留 QQ 家园 WAP 时代体验、但能在现代设备运行的文字多人 RPG。
>
> 资料说明：旧游戏公开资料并不完整。文中把能够从旧攻略互相印证的内容标为“原版骨架”，具体数值与缺失规则作为“复刻建议”，后续应通过旧截图、录像和老玩家访谈校准。

## 1. 产品定义

### 1.1 一句话概念

玩家从威尼斯出发，通过主线任务、跨城旅行、野外战斗和多人副本不断升级，收集并强化经典套装，加入帮会争夺城市，最终成为全服知名强者。

### 1.2 设计支柱

1. **文字也要有世界感**：地点以“城市 → 城门 → 郊外 → 区域 → 怪物/事件”的链接树展开。
2. **每次点击都有反馈**：一次战斗、一次移动、一次开箱，都马上返回清楚的文字结果。
3. **长期追求可见**：等级、套装、强化、宝石、宠物、帮会构成长线成长。
4. **社会关系制造记忆**：组队、交易、帮会、攻城、排行比单机数值更重要。
5. **低操作门槛，高数值策略**：不考验手速，重点是装备选择、资源安排、挑战时机和社交协作。

### 1.3 不建议首版加入的内容

- 3D 地图、实时走路与摇杆操作；
- 复杂舰队操控或即时海战；
- 开放式玩家定价交易所；
- 大量职业和技能树；
- 首发就做到原版全部等级与副本。

这些内容会稀释文字游戏的核心，并大幅提高内容和服务器成本。

## 2. 核心循环

### 2.1 分钟级循环

```text
查看当前位置
  → 选择区域或怪物
  → 战斗结算
  → 获得经验、银币、材料、装备
  → 使用药品/更换装备
  → 继续深入或回城
```

### 2.2 小时级循环

```text
接任务 → 前往目标城市/区域 → 刷怪或打 Boss
→ 完成任务 → 升级/解锁功能 → 强化装备 → 挑战更高区域
```

### 2.3 周级循环

```text
日常积累体力与材料
→ 按开放日挑战副本
→ 凑齐套装/宝石/宠物资源
→ 参加帮战与城市争夺
→ 提升排行榜位置
```

### 2.4 长线循环

等级成长只是门槛；长期目标应依次转为：

1. 凑齐阶段套装；
2. 提高强化等级和宝石品质；
3. 培养宠物与技能搭配；
4. 完成高难副本；
5. 加入强帮、争夺城市与稀有区域；
6. 追求收藏、称号和赛季荣誉。

## 3. 世界与移动逻辑

### 3.1 地图不是图片，而是一张有向图

每个地点是一个 `Location` 节点，出口是 `LocationEdge`。客户端只展示玩家当前位置允许访问的出口。

```text
威尼斯
├── 城内
│   ├── 商店
│   ├── 铁匠/功能 NPC
│   └── 码头
├── 北城门
│   └── 威尼斯郊外
│       └── 威尼斯探险副本
└── 东城门
    └── 更高级区域
```

地点节点至少包含：

| 字段 | 用途 |
|---|---|
| `id` | 稳定唯一标识 |
| `name` | 显示名称 |
| `type` | 城市、野外、副本、商店、功能点 |
| `description` | 当前页面的氛围文字 |
| `min_level` | 最低等级 |
| `safe_zone` | 是否禁止攻击 |
| `encounter_table_id` | 怪物/事件表 |
| `services` | 商店、强化、仓库等功能 |

出口至少包含：起点、终点、移动方式、银币费用、体力费用、耗时、等级/任务条件。

### 3.2 两类长途移动

- **出航**：便宜或免费，但有等待时间/体力成本，可触发随机事件；适合保留原味。
- **传送**：立即抵达但消耗银币；帮会占领城市后可对成员减免。

移动请求必须由服务端校验：玩家当前位置、是否在战斗、目标出口是否存在、费用是否足够、条件是否满足。不能让客户端直接写当前位置。

## 4. 角色与成长

### 4.1 首版基础属性

建议先做一套易解释的属性，后续再按原版资料调整：

| 属性 | 作用 |
|---|---|
| 等级 `level` | 内容门槛和基础成长 |
| 生命 `hp/max_hp` | 归零即战败 |
| 体力 `energy` | 限制连续出航或挂机收益 |
| 攻击 `attack` | 提高直接伤害 |
| 防御 `defense` | 降低受到的伤害 |
| 敏捷 `speed` | 决定先手，并小幅影响闪避 |
| 暴击 `crit_rate` | 造成额外伤害的概率 |
| 命中 `accuracy` | 对抗闪避 |
| 抗性集合 | 抗麻痹、反弹、封锁等中后期机制 |

最终属性只在服务端计算：

```text
最终属性 = 等级基础值
         + 已装备物品基础值
         + 套装效果
         + 强化效果
         + 宝石效果
         + 宠物/称号/帮会效果
```

不要把最终值永久写死在角色表里；应保留来源，便于换装备、回滚和查作弊。

### 4.2 经验曲线

首个可玩版本建议只开放 1–30 级：

```text
升级经验 = round(100 × level^1.65)
```

这是调试起点，不是最终原版公式。设计目标：

- 1–5 级：10 分钟内完成；
- 5–15 级：约 1–2 小时；
- 15–30 级：约 1–3 天自然游玩；
- 30 级之后再进入多人副本、帮会与深度养成。

### 4.3 资源体系

首版只保留三层资源，避免失控：

- **银币**：商店、传送、基础强化、交易税；
- **绑定材料**：任务和副本产出，用于装备成长，不能玩家间转移；
- **高级货币**：若未来商业化再引入，不能直接售卖顶级战力。

银币的产出与回收必须配对。主要回收口：传送、药品、强化、宝石合成、帮会建设、交易税。

## 5. 战斗系统

### 5.1 形式

战斗在服务端一次性模拟完毕，客户端播放文字战报。玩家看到的是逐回合记录，但不需要实时等待服务器逐秒执行。

```text
第1回合：你挥动短剑，对海盗造成 23 点伤害。
第1回合：海盗反击，对你造成 8 点伤害。
第2回合：你触发暴击，造成 41 点伤害。
战斗胜利！获得经验 18、银币 7、破旧布料×1。
```

### 5.2 建议的首版公式

```text
命中率 = clamp(0.75 + (攻击者命中 - 防守者闪避) / 200, 0.20, 0.98)

基础伤害 = max(1, 攻击者攻击 × 技能倍率 - 防守者防御 × 0.55)

最终伤害 = round(基础伤害 × random(0.90, 1.10))

暴击伤害 = 最终伤害 × 1.5
```

首版只做：普通攻击、暴击、闪避、一个主动技能、药品。麻痹、反弹、吸血、卸装、封锁等机制放到第二阶段。

### 5.3 战斗状态机

```text
VALIDATE（校验玩家/怪物/次数）
→ PREPARE（生成双方快照与随机种子）
→ ROUND_START
→ PLAYER_ACTION
→ ENEMY_ACTION
→ CHECK_END
   ├── 未结束：返回 ROUND_START
   ├── 玩家胜利：REWARD
   ├── 玩家失败：DEFEAT
   └── 达到回合上限：DRAW
→ COMMIT（同一事务保存血量、掉落、任务进度和日志）
```

必须设最大回合数，例如 50 回合，防止双方无法破防造成死循环。

### 5.4 随机数与防重复领奖

- 服务端生成随机种子并记入战斗记录；
- 每次请求带 `request_id`，重复提交只返回同一份结果；
- 奖励发放、任务计数、体力扣除必须处于同一数据库事务；
- 客户端只发送“挑战哪个怪”，不能发送“掉了什么”。

## 6. 装备、套装、强化与宝石

### 6.1 装备模型

将“物品定义”和“玩家拥有的实例”分开：

- `ItemDefinition`：名称、部位、等级、品质、基础属性、可否交易；
- `ItemInstance`：归属玩家、强化等级、耐久、镶嵌宝石、绑定状态。

推荐部位：手持、头戴、身穿、鞋、左饰品、右饰品。首版不要做太多部位。

### 6.2 套装路线

旧攻略能确认的代表性阶段包括武士、哥伦布/月、霸者、地魔、天魔、海誓山盟等。首版只实现：

```text
1–5级 新手散件
5级 武士套（首个副本目标）
15级 城市散件
30级 哥伦布套 / 月套（二选一构筑方向）
```

套装加成采用数据配置：2 件、4 件、6 件分别触发，不把规则写死在代码中。

### 6.3 强化

原味方向可以保留：低级强化仅花银币，高级强化需要材料、图纸与保护/增幅道具。

首版建议：

- +1～+3：100% 成功；
- +4～+6：失败不掉级，只消耗材料；
- +7 以上：第二阶段再开放；
- 同一装备每天的图纸领取/挑战次数由服务端日重置控制。

必须展示成功率和失败损失，不能使用模糊措辞。

### 6.4 宝石

- 宝石有种类、等级、允许镶嵌部位；
- 3 颗低级宝石合成 1 颗高级宝石；
- 合成结果与消耗在单一事务中完成；
- 抗性宝石属于 PVP 后期内容，首版先做攻击、防御、生命三类。

## 7. 任务、副本与掉落

### 7.1 任务系统

任务由条件与奖励组成，不为每个任务单独写脚本。

支持的首版条件：

- 到达地点；
- 与 NPC 对话；
- 击败某类怪物 N 次；
- 获得/提交物品 N 个；
- 达到某等级；
- 通关某副本。

任务状态：`AVAILABLE → ACCEPTED → COMPLETED → CLAIMED`。奖励只能在 `COMPLETED` 到 `CLAIMED` 的原子更新中发放一次。

### 7.2 副本结构

副本是一个有次数、开放时间和独立掉落表的地点链。旧攻略可确认的代表内容包括威尼斯探险、贝河、五行阵、巨鲸、诸葛、基德的宝藏、天魔之乱和情侣副本。

首版只做 **威尼斯探险**：

```text
入口校验（≥5级、今日有次数）
→ 小怪节点 A/B 二选一
→ 精英怪
→ 休整/宝箱
→ Boss
→ 结算并记录通关
```

掉落表字段：对象类型、对象 ID、权重、数量范围、保底计数、是否绑定、队伍分配规则。

### 7.3 组队

组队不是首个单机原型的阻塞项。联网 MVP 再实现：

- 队长发起副本；
- 服务端锁定队伍快照；
- 队员按敏捷排序行动，怪物选择目标；
- 掉落先采用个人独立掉落，避免需求/贪婪系统的争议；
- 离线玩家可由系统托管普通攻击。

## 8. 宠物、帮会、PVP

### 8.1 宠物

宠物属于中期构筑系统：等级、资质、技能槽、封锁技。旧攻略提到宠物每 5 级领悟技能、40 级形成完整技能组；复刻时应先做简化版：

- 每名玩家出战 1 只宠物；
- 宠物在玩家行动后行动；
- 只有 3 个技能槽；
- 资质只影响基础属性，不在首版暗改技能概率。

### 8.2 帮会与城市占领

帮会的真正价值应是共同目标，而不只是属性面板：

- 捐献资源升级帮会；
- 帮会商店；
- 城市传送费减免；
- 占领特殊地点后提高特定掉落；
- 周末固定时段攻城；
- 赛季结束重置占领，保留荣誉记录。

攻城首版可用“异步贡献战”实现：成员在活动时间挑战守军，累计伤害/积分决定胜负，不必实现实时百人对战。

### 8.3 PVP 原则

- PVE 与 PVP 数值倍率分离；
- 控制技能必须有命中率、持续回合和抗性；
- 不允许无限控制链；
- 竞技场使用每日次数和分段匹配；
- 战报保存随机种子与属性快照，便于仲裁。

## 9. 页面与交互

### 9.1 主页面信息层级

```text
【威尼斯·北城门】
你站在古老城墙下，远处传来码头的钟声。

生命 84/100　体力 23/30
等级 5　经验 120/180　银币 362

可前往：
[城内] [威尼斯郊外] [威尼斯探险]

附近：
[流浪水手 Lv.4] [码头恶徒 Lv.6]

[任务] [背包] [角色] [队伍] [帮会]
```

### 9.2 交互规则

- 一屏只突出一个主要决定；
- 链接/按钮名称直接表达结果；
- 战报默认显示摘要，可展开完整回合；
- 永远提供“返回当前位置”；
- 危险、消费、丢弃操作二次确认；
- 文本字号和触控区域适合手机，不追求复刻功能机的小字。

## 10. 技术架构

### 10.1 推荐架构

```text
Godot 4 客户端（Control UI）
       │ HTTPS / WebSocket
       ▼
游戏 API 服务（权威规则与鉴权）
       ├── PostgreSQL（永久数据）
       ├── Redis（会话、限流、排行榜、短期锁）
       └── 后台任务（每日重置、活动、邮件、赛季结算）
```

建议选型：

- 客户端：Godot 4.7.1，全部使用 `Control` 节点；
- 服务端：Python FastAPI；
- 数据库：PostgreSQL；
- 缓存/队列：Redis，首个原型可暂不引入；
- 数据配置：CSV/JSON 导入数据库；
- 部署：Docker Compose 起步，正式环境再拆服务。

### 10.2 为什么不能只用 Godot 存档

纯单机试玩可以用本地 `Resource/JSON`，但多人版的等级、货币、装备、掉落、战斗和交易必须由服务端决定。否则修改本地存档或伪造请求即可刷出顶级装备。

### 10.3 Godot 客户端结构

```text
res://
├── scenes/
│   ├── bootstrap.tscn
│   ├── login.tscn
│   ├── game_shell.tscn
│   └── panels/
│       ├── location_panel.tscn
│       ├── battle_report_panel.tscn
│       ├── inventory_panel.tscn
│       ├── character_panel.tscn
│       └── quest_panel.tscn
├── scripts/
│   ├── autoload/api_client.gd
│   ├── autoload/session.gd
│   ├── autoload/event_bus.gd
│   ├── models/
│   └── ui/
├── themes/wap_theme.tres
└── data/（仅原型配置）
```

不要为每个城市创建一个 Godot 场景。城市、地点、怪物和任务都是数据，统一由通用面板渲染。

### 10.4 服务端模块

```text
server/
├── app/main.py
├── auth/
├── players/
├── world/
├── combat/
├── inventory/
├── quests/
├── dungeons/
├── guilds/
├── economy/
├── admin/
└── tests/
```

初期是“模块化单体”，不要一开始拆微服务。边界清楚以后再按压力拆分。

### 10.5 核心 API

| 方法 | 路径 | 用途 |
|---|---|---|
| `POST` | `/auth/guest` | 建立测试账号 |
| `GET` | `/me` | 角色摘要 |
| `GET` | `/world/current` | 当前地点、出口、怪物、NPC |
| `POST` | `/world/move` | 移动或出航 |
| `POST` | `/combat/start` | 挑战怪物并返回战报 |
| `GET` | `/inventory` | 背包 |
| `POST` | `/equipment/equip` | 穿戴装备 |
| `POST` | `/items/use` | 使用物品 |
| `GET` | `/quests` | 任务列表 |
| `POST` | `/quests/{id}/accept` | 接任务 |
| `POST` | `/quests/{id}/claim` | 领奖 |
| `POST` | `/dungeons/{id}/enter` | 进入副本 |

所有改变状态的接口都要支持幂等键，并进行频率限制。

## 11. 最小数据库模型

### 11.1 玩家域

- `users`
- `characters`
- `character_stats`
- `currencies`
- `energy_ledger`

### 11.2 内容域

- `locations`
- `location_edges`
- `npc_definitions`
- `monster_definitions`
- `encounter_tables`
- `item_definitions`
- `set_definitions`
- `quest_definitions`
- `dungeon_definitions`
- `drop_tables`

### 11.3 玩家状态域

- `item_instances`
- `inventory_slots`
- `equipment_slots`
- `quest_progress`
- `dungeon_attempts`
- `combat_records`
- `reward_ledger`

`reward_ledger` 很重要：每笔货币、道具、经验的来源和去向都应可追溯。

## 12. 内容数据化示例

怪物配置示例：

```json
{
  "id": "venice_dock_thug_01",
  "name": "码头恶徒",
  "level": 5,
  "stats": {"hp": 70, "attack": 14, "defense": 6, "speed": 8},
  "exp_reward": 18,
  "silver_range": [4, 9],
  "drop_table_id": "venice_common_lv5",
  "skills": ["normal_attack"]
}
```

任务配置示例：

```json
{
  "id": "main_001_dock_cleanup",
  "name": "码头的麻烦",
  "prerequisites": [],
  "objectives": [
    {"type": "kill", "target": "venice_dock_thug_01", "count": 3}
  ],
  "rewards": {
    "exp": 80,
    "silver": 40,
    "items": [{"id": "small_milk_bottle", "count": 2}]
  },
  "next_quest_id": "main_002_north_gate"
}
```

## 13. 开发顺序

### 阶段 A：离线垂直切片（约 1–2 周）

目标：证明文字界面和成长循环好玩。

- Godot 主界面与 WAP 风格主题；
- 1 个城市、5 个地点、3 种怪物；
- 1–5 级；
- 战斗、经验、掉落、背包、穿戴；
- 5 条任务；
- 本地存档。

验收：新玩家不用讲解，能在 15 分钟内完成任务、升级、换装并击败第一个精英怪。

### 阶段 B：联网 MVP（约 3–5 周）

- FastAPI + PostgreSQL；
- 登录与角色；
- 服务端权威战斗；
- 内容配置导入；
- 任务、背包、装备；
- 战斗与奖励日志；
- 基础后台管理。

验收：客户端删档/改包不能改变服务器资产；重复请求不能重复领赏。

### 阶段 C：首个完整章节（约 4–8 周）

- 1–30 级；
- 威尼斯及相邻城市；
- 武士套、哥伦布/月套；
- 强化 +1～+6；
- 基础宝石；
- 威尼斯探险与贝河副本；
- 世界聊天、好友、队伍。

### 阶段 D：社区长线

- 帮会、帮会商店、异步攻城；
- 宠物；
- 玩家交易；
- 更多城市和周常副本；
- 赛季、排行榜、运营活动。

## 14. 第一版明确范围

第一版建议锁死以下范围，做完再扩：

```text
平台：macOS/Windows 调试，后续导出 Android
地图：威尼斯城内、北城门、码头、郊外、副本入口
等级：1–5
怪物：3 普通 + 1 精英 + 1 Boss
装备：6 部位，普通/优秀/武士套
系统：战斗、任务、掉落、背包、穿戴、商店、存档
副本：威尼斯探险
暂不做：宠物、帮会、PVP、交易、充值、实时聊天
```

## 15. 制作原则与风险

1. **先还原节奏，再还原数量**：先做 5 级好玩的切片，不要先录入 140 级装备表。
2. **内容和代码分离**：策划改怪物、掉落和任务时不应改代码。
3. **服务端权威**：多人版的资产变更永远由服务端提交。
4. **经济留流水**：任何奖励和消费都可追溯，才能处理漏洞与回档。
5. **先个人掉落**：多人共享掉落会迅速带来纠纷与外挂收益。
6. **谨慎复刻名称与素材**：公开发行前需处理腾讯标识、原文案、美术、音乐和游戏名称的知识产权问题。可以复刻玩法精神，但应制作原创世界观与内容。
7. **老玩家验证**：每完成一个阶段，邀请 3–5 名老玩家核对“像不像”，重点询问路径、节奏、术语和数值感受。

## 16. 下一步可直接执行的任务

按顺序创建：

1. Godot 项目和 `game_shell.tscn`；
2. WAP 蓝白/绿白主题；
3. 通用地点面板，由 JSON 渲染地点、出口和怪物；
4. 本地角色与背包模型；
5. 确定性战斗模拟器与文字战报；
6. 威尼斯 1–5 级内容；
7. 本地存档；
8. 试玩并调节前 15 分钟体验；
9. 再把规则迁到 FastAPI 服务端。

